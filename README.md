# Tarot-Reading-Platform
The core loop is:  User registers / logs in (email)  User draws tarot cards (random logic)  System sends cards + user context to AI agent  AI returns an interpretation  User reads result (possibly pays to unlock)
